import { Routes } from '@angular/router';
import {DiceSystemComponent} from './dice-system/dice-system.component';

export const routes: Routes = [
    {path: '', component: DiceSystemComponent},


];
